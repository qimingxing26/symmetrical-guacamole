# design 目录

这里放你的原型文件。

推荐做法：

1. 先根据 `memory-bank/PRD.md` 生成首页、作品详情页、管理员登录页、评论管理页原型。
2. 原型可以是 HTML，也可以是图片，但 HTML 原型更方便后续还原。
3. 原型定稿后，再让 AI 基于原型补充 `memory-bank/DESIGN.md`。

建议文件名：

```text
design/index.html
design/project.html
design/login.html
design/admin.html
```
